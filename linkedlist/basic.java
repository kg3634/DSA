/*
ARRAY(array.png):int [] arr = new int[4] would take 40 bytes(4 bytes per integer) and would be created only when continous 40
bytes memory is present in heap.
LINKEDLIST(linkedlist.png): Though it would take double bytes(80 bytes - 40 for integer  and 40 for reference), but it can be created
even if continous memory is not availbale in heap.
[linkedlist2.png]---There are 2 parts in a node: Data and Address of the next node.
linkedlist3.png -- check what contains inside linked liks(head ,tail,size)

*/
package DSA.linkedlist;

public class basic {
    
}
